# Infrastructure layout

- `modules/tool`: reusable module for baseline tool infra
- `live/hathor-red`: project-specific instantiation

Use Cloud Shell for inventory and apply flows to ensure `gcloud` availability.
